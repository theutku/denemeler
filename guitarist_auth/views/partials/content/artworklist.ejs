<article class="artworklist">
    <h1>Gallery</h1>
    <p>While you are taking a tour on our site, head over to the gallery where you can see some iconic photos of the guitarists.</p>
    <div class="pixgrid">
        <% if(artwork.length > 0) { %>
            <% for(i=0; i<artwork.length; i++) { %>
                <img src="/images/artwork/<%= artwork[i] %>" alt="Artwork <%= i %>">
            <% } %>
        <% } %>
    </div>
</article>